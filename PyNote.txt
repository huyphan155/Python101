Week1.

Day1 :
    variable
    string
    int/float/bool
    print
    input

TASK : convert temp, average, parse simple string.

Day2:
    if / else
    for
    while
    function

TASK: check log contain error, count number of appear, simple CLI menu

##
for char in "UART":
    print(char)

for i in range(1, 6):
    print(i) -> 1,2,3,4,5

if "ERROR" in "ERROR: UART timeout":
        print("Found ERROR")

Day3:
    list = [ "struct", ]
    dict { "level": "ERROR", }
    tuple ( "immutable" )   ( = list but immutable )
    set  {"Error", "Error" }  => no duplicate => print(set) = Error

TASK: parse log to dictionary, message frequency

Week2.

Day1:
    open / read / write file
    pathlib
    exception

file = open("logs.txt", "r") / data = file.read() / file.write("Hello Python")

TASK : read log file, filter WARNING / ERROR, export new file

Day2:
    import
    pip
    virtualenv

Task: package install, use package, read simple COM port (pyserial)

Day3:
    regex      # import re
    argparse   # import argparse
    logging    # import logging

Task: Parse log with Regex, CLI receive input file, write log to file

Week3:

Day1: script → structured data
    class
    object
    dataclass

Task: Log entry class, parser return object, list object + frequency