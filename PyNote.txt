Python Learning Roadmap
=======================
===============================================================================================
                                           WEEK 1
===============================================================================================

Day 1
--------------------
Topics:
- Variables
- String
- Data types:
    - int
    - float
    - bool
- print()
- input()

Tasks:
- Temperature converter
- Calculate average value
- Parse simple string


Day 2
--------------------
Topics:
- if / else
- for loop
- while loop
- function

Tasks:
- Check log contains "ERROR"
- Count occurrences
- Simple CLI menu

Examples:

for char in "UART":
    print(char)

for i in range(1, 6):
    print(i) -> 1,2,3,4,5

if "ERROR" in "ERROR: UART timeout":
    print("Found ERROR")


Day 3
-----------------------
Topics:

- list = [ "struct", ]
- dict { "level": "ERROR", }
- tuple ( "immutable" )   ( = list but immutable )
- set  {"Error", "Error" }  => no duplicate => print(set) = Error

Tasks:
- Parse log to dictionary
- Count message frequency

===============================================================================================
                                            WEEK 2
===============================================================================================

Day 1
---------------------
Topics:
- Open / Read / Write file
- pathlib
- Exception handling

Example:

file = open("logs.txt", "r")
data = file.read()
file.write("Hello Python")

Tasks:
- Read log file
- Filter WARNING / ERROR
- Export result to new file


Day 2
------------------------------
Topics:
- import
- pip
- virtualenv

Tasks:
- Install package
- Use package
- Read simple COM port using pyserial


Day 3
-----------------------
Topics:
- regex       (import re)
- argparse    (import argparse)
- logging     (import logging)

Tasks:
- Parse log with Regex
- CLI receive input file
- Write log to file

===============================================================================================
                                            WEEK 3
===============================================================================================

Day 1: script → structured data
------------------
Topics:
- class
- object
- dataclass

Tasks:
- Create LogEntry class
- Parser returns object
- Store list of objects
- Count frequency

Day 2:
------------------
Topics:
- inheritance
- composition
- project structure

Tasks:
- ErrorLog(LogEntry)
- InfoLog(LogEntry)

Day 3:
------------------
Topics:
- Polymorphism
- Project split (models.py, parser.py, manager.py, main.py)

Tasks:
- Project split (models.py, parser.py, manager.py, main.py)

===============================================================================================
                                            WEEK 4
===============================================================================================

Day 1:
------------------
Topics:
- JSON ( similar with dict in python )
- serialize / deserialize : json.dumps(data) / json.loads(json_data)
- R/W JSON file

DIFF:
    JSON: true - null
    Python: True - None

SERIALIZE : Python object → JSON
    import json
    data = {
        "level":"ERROR",
        "message":"UART timeout"
    }
    json_data = json.dumps(data) # {"level":"ERROR","message":"UART timeout"}

DESERIALIZE : JSON → Python
    json_data = """
    {
        "level":"ERROR",
        "message":"UART timeout"
    }
    """
    data = json.loads(json_data) # {'level':'ERROR', 'message':'UART timeout' }

Phan biet : json.loads() (Text in json format) vs json.load() ( file object )


Tasks: Working with JSON

Day 2:
------------------
Topics:
- HTTP
- REST API
- requests
- GET
- POST

HTTP: Communication between client and server.
    Python script
        ↓
    GET https://api.site.com/logs
        ↓
    Server
        ↓
    JSON response

Day 3:
------------------
Topics: simple log pipeline

        logs.txt
            ↓
        parse_log()
            ↓
        LogManager
            ↓
        count_frequency()
            ↓
        result.json
            ↓
        upload frequency
            ↓
        upload all logs

===============================================================================================
                                            WEEK 5
===============================================================================================

Day 1:
------------------
Topics:
- what is UV, poetry?
- pip
- virtualenv (.venv)
- requirements.txt
- vì sao cần môi trường ảo

pip freeze > requirements.txt
print(requests.__version__)
print(requests.__file__)


Day 2: UV
------------------
install : powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
Check   : uv --version

# Create new folder and install with uv
mkdir uv_demo
cd uv_demo
uv init
uv add requests

(.venv) PS D:\GitWork\Python101\uv_demo> pip freeze
certifi==2026.5.20
charset-normalizer==3.4.7
idna==3.16
pyserial==3.5
requests==2.34.2
urllib3==2.7.0

(.venv) PS D:\GitWork\Python101\uv_demo> uv tree
Resolved 6 packages in 1ms
uv-demo v0.1.0
└── requests v2.34.2
    ├── certifi v2026.5.20
    ├── charset-normalizer v3.4.7
    ├── idna v3.17
    └── urllib3 v2.7.0

only install requests but got all the dependency, uv show tree make us understand the dependency

Day 3: package, module
---------------------

Week5/
│
├── main.py
│
└── logtool/
    ├── __init__.py
    ├── parser.py
    ├── models.py
    └── ...

outSite package (main.py ) : from logtool.models import ErrorLog
inside package  (parser.py) : from .models import ErrorLog

===============================================================================================
                                            WEEK 6
===============================================================================================

Task 1: Exception Handling
------------------
-
try:
    print("work")
except:
    print("error")
finally:                                  # always run
    print("always run")

-
try:
    ...
except Exception as e:
    print(e)

Task 2: Pytest + mock
------------------

open cmd and run : pytest
    - py test work by collect all the name with "test_*.py / *_test.py" in the folder
    - inside def must have format:
            def test_abc():

===============================================================================================
                                            WEEK 6 - 7
===============================================================================================

Task 1:
------------------
MagicMock
side_effect
assert_called_once
call_count

===============================================================================================
                                            WEEK 8
===============================================================================================
Packaging
CLI Tool
Typer
Project Structure

Command
- normal command ( only Argument )
    @app.command()
    def hello():
        print("Hello World")
- Option
    @app.command()
    def greet(
        name: str = typer.Argument(...),
        upper: bool = typer.Option(False)
    ):
        if upper:
            print(name.upper())
        else:
            print(name)

=> run python cli.py greet --help

Typer auto generate doc => ADVANCE

   ex :

    (.venv) PS D:\GitWork\Python101\Week8_packaging> python cli.py greet --help

     Usage: cli.py greet [OPTIONS] NAME

    ╭─ Arguments ─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╮
    │ *    name      TEXT  [required]                                                                                                                 │
    ╰─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╯
    ╭─ Options ───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╮
    │ --upper    --no-upper      [default: no-upper]                                                                                                  │
    │ --help                     Show this message and exit.
    ╰─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╯


___________________________________________________________________________________________
ADD to pyproject.toml

    [project.scripts]
    logtool = "logtool_W8.cli:app"

then Run : pip install -e .
-> pip create : .venv\Scripts\logtool.exe

So when user type : logtool hello ( equal : from logtool_W8.cli import app + app() )


(.venv) PS D:\GitWork\Python101\Week8_packaging> logtool --help

 Usage: logtool [OPTIONS] COMMAND [ARGS]...

╭─ Options ───────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╮
│ --install-completion          Install completion for the current shell.                                                                         │
│ --show-completion             Show completion for the current shell, to copy it or customize the installation.                                  │
│ --help                        Show this message and exit.                                                                                       │
╰─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Commands ──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╮
│ hello                                                                                                                                           │
│ greet                                                                                                                                           │
│ show-config                                                                                                                                     │
│ add                                                                                                                                             │
│ repeat                                                                                                                                          │
╰─────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╯

===============================================================================================
                               Week 9 — Logging & Configuration
===============================================================================================
logger
    DEBUG : logger.debug(f"Input path = {path}")
    INFO  : logger.info("Program started")
    WARNING : logger.warning("Battery low")
    ERROR : logger.error("Upload failed")
    CRITICAL : logger.critical("Database corrupted")
handler
formatter
file handler
console handler
log level

    ex :
def setup_logger():
    # create logger name 'logtool'
    logger = logging.getLogger("logtool")

    # setup level to INFO
    logger.setLevel(logging.INFO)

    # avoid duplicate
    if not logger.handlers:
        # "Formatter"
        formatter = logging.Formatter("[%(asctime)s] [%(levelname)s] - %(message)s", datefmt="%Y-%m-%d %H:%M:%S")

        # "Handler 1" for Terminal (StreamHandler)
        console_handler = logging.StreamHandler()
        console_handler.setFormatter(formatter)
        logger.addHandler(console_handler)

        # "Handler 2" for write to 'tool.log' (FileHandler)
        file_handler = logging.FileHandler("tool.log", encoding="utf-8")
        file_handler.setFormatter(formatter)
        logger.addHandler(file_handler)

    return logger

Config
├── config.json
├── load_config()

Environment
├── os.getenv()
├── .env
├── python-dotenv

===============================================================================================
                                   Week 10 — Async Python
===============================================================================================
async def
await
asyncio.run()
asyncio.gather()

what is different between "def" and "async def"?
what "await" is truly do ?

ex :
    import asyncio

    async def task1():
        print("Task1 Start")

        await asyncio.sleep(5)

        print("Task1 Done")

    async def task2():
        print("Task2 Start")

        await asyncio.sleep(5)

        print("Task2 Done")

    async def main():
        await asyncio.gather(
            task1(),
            task2()
        )

    asyncio.run(main())

( await : 1 thread manage by event loop ) # multi thread

Async Function = Normal Function + can WAIT

Purpose of async :
    HTTP Request
    Database
    Message Queue
    WebSocket
